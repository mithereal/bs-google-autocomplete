open Css;

let label = style([
fontWeight(400),
textAlign(`right),
width(px(100)),
color(grey)
]);

let field = style([
width(`percent(99.))
]);

let slimField = style([
width(px(80))
]);

let wideField = style([
width(px(200))
]);